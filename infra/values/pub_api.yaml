app: pub-api
deployment:
  enable: true
  replicas: 2
service:
  enable: true
  port: 80
  targetPort: 3000
  type: LoadBalancer
containers:
    image: pradeep800/pub_api:latest
    containerPort: 3000
secrets:
  - tasks-secrets
  - s3-secrets
# no cron job
cron_job:
  enable: false 
postgres_statefulset:
  enable: false
volume:
  enable: false
